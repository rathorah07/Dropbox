#ifndef STRING_INT_H
#define STRING_INT_H

#include <iostream>
#include <string>
using namespace std;

string convert_to_string(long long);

long long  convert_to_int(string);

string word_to_string(char A[],int n);

bool is_string_zero(string);


#endif // STRING_INT_H
